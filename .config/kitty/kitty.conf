# kitty não estava instalado/configurado ainda nesta máquina — este é o
# primeiro config, já com o tema Gruvbox Dark como padrão.
# Troque o include abaixo para mudar de tema.

include themes/gruvbox-dark.conf

font_family      JetBrainsMono Nerd Font Mono
font_size        12.0

confirm_os_window_close 0
